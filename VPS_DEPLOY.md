# 🚀 Развертывание на VPS через Termius

## 📋 Подготовка

### Что понадобится:
- VPS сервер (Ubuntu/Debian)
- Termius установлен на вашем компьютере
- Доступ к серверу (SSH ключ или пароль)
- Git репозиторий с проектом (или готовые файлы)

---

## 🔌 ШАГ 1: Подключение к VPS через Termius

1. **Откройте Termius**
2. **Создайте новое подключение:**
   - Нажмите `+` или `New Host`
   - Введите:
     - **Hostname**: IP адрес вашего VPS (например: `123.45.67.89`)
     - **Port**: `22` (стандартный порт SSH)
     - **Username**: обычно `root` или `ubuntu`
   - Сохраните подключение

3. **Подключитесь к серверу:**
   - Выберите созданное подключение
   - Введите пароль или используйте SSH ключ
   - Вы должны увидеть приглашение командной строки

---

## 🐳 ШАГ 2: Установка Docker и Docker Compose

### 2.1 Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 2.2 Установка Docker

```bash
# Установка необходимых пакетов
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Добавление официального GPG ключа Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавление репозитория Docker
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установка Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

# Проверка установки
docker --version
docker compose version
```

**Если возникли ошибки**, используйте упрощенную установку:

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
```

### 2.3 Добавление пользователя в группу docker

```bash
sudo usermod -aG docker $USER
```

**Важно**: После этого нужно **переподключиться** к серверу, чтобы изменения вступили в силу.

---

## 📦 ШАГ 3: Загрузка проекта на VPS

### Вариант A: Через Git (рекомендуется)

```bash
# Установка Git (если еще не установлен)
sudo apt install -y git

# Создание папки для проекта
mkdir -p ~/projects
cd ~/projects

# Клонирование репозитория (замените URL на ваш)
git clone https://github.com/yourusername/tg-zodiac.git
# ИЛИ
git clone git@github.com:yourusername/tg-zodiac.git

# Переход в директорию проекта
cd tg-zodiac
```

### Вариант B: Через SCP (если нет Git)

**В Termius есть встроенный SFTP клиент:**

1. В Termius выберите ваше подключение
2. Нажмите на иконку **SFTP** (папка с замочком)
3. Перетащите папку проекта из локального компьютера в `~/projects/tg-zodiac`

**Или через командную строку на вашем компьютере:**

```bash
# Создайте архив проекта (на вашем компьютере)
tar -czf tg-zodiac.tar.gz tg-zodiac --exclude='.git' --exclude='__pycache__' --exclude='*.db'

# Загрузите на сервер
scp tg-zodiac.tar.gz user@your-vps-ip:~/

# На сервере распакуйте
ssh user@your-vps-ip
cd ~
mkdir -p projects
cd projects
tar -xzf ~/tg-zodiac.tar.gz
cd tg-zodiac
```

---

## ⚙️ ШАГ 4: Настройка .env файла

```bash
# Создайте .env файл
nano .env
# ИЛИ
vi .env
```

**Вставьте следующее содержимое** (замените значения на свои):

```env
TG_TOKEN=8224594765:AAEHJARxpA8iYbdvf3nvRThg1Bc9ppf-o8k
ADMIN_ID=375961707,498458650
DAILY_HOUR=9
DAILY_MINUTE=0
```

**Как редактировать в nano:**
- Вставьте текст (Ctrl+Shift+V или правая кнопка мыши)
- Сохраните: `Ctrl+O`, затем `Enter`
- Выйдите: `Ctrl+X`

**Как редактировать в vi:**
- Нажмите `i` для входа в режим вставки
- Вставьте текст
- Нажмите `Esc`, затем `:wq` и `Enter` для сохранения и выхода

**Установите правильные права доступа:**

```bash
chmod 600 .env
```

---

## ✅ ШАГ 5: Проверка файлов проекта

Убедитесь, что все необходимые файлы на месте:

```bash
ls -la

# Должны быть:
# - Dockerfile
# - docker-compose.yml
# - bot.py
# - requirements.txt
# - data/predictions.json
# - .env
```

Если файла `data/predictions.json` нет, создайте его:

```bash
mkdir -p data
# Скопируйте файл или создайте заново
```

---

## 🚀 ШАГ 6: Запуск проекта

### 6.1 Сборка и запуск

```bash
# Сборка и запуск всех контейнеров
docker compose up -d --build

# Или если используете старую версию docker-compose:
docker-compose up -d --build
```

### 6.2 Проверка статуса

```bash
# Статус контейнеров
docker compose ps

# Должны быть запущены:
# - zodiac_bot
# - zodiac_postgres
```

### 6.3 Просмотр логов

```bash
# Логи бота
docker compose logs -f bot

# Чтобы выйти из просмотра логов, нажмите Ctrl+C
```

**Если видите ошибки**, проверьте:

```bash
# Все логи
docker compose logs

# Логи PostgreSQL
docker compose logs postgres
```

---

## 🔍 ШАГ 7: Проверка работоспособности

1. **Найдите вашего бота в Telegram**
2. **Отправьте команду `/start`**
3. **Проверьте логи:**

```bash
docker compose logs --tail=50 bot
```

4. **Проверьте базу данных:**

```bash
docker compose exec postgres psql -U bot -d botdb -c "SELECT COUNT(*) FROM users;"
```

---

## 📝 Основные команды для управления

### Просмотр логов

```bash
# Логи бота в реальном времени
docker compose logs -f bot

# Последние 100 строк логов
docker compose logs --tail=100 bot

# Логи с ошибками
docker compose logs bot | grep -i error
```

### Перезапуск

```bash
# Перезапуск бота (после изменения кода)
docker compose up -d --build --force-recreate bot

# Перезапуск всех сервисов
docker compose restart
```

### Остановка

```bash
# Остановка всех контейнеров
docker compose stop

# Остановка и удаление контейнеров
docker compose down
```

### Обновление кода

```bash
# Если используете Git:
cd ~/projects/tg-zodiac
git pull

# Пересборка и перезапуск
docker compose up -d --build --force-recreate bot

# Проверка логов
docker compose logs -f bot
```

---

## 🔧 Решение проблем

### Бот не запускается

```bash
# Проверить логи
docker compose logs bot

# Проверить переменные окружения
docker compose exec bot env | grep TG_TOKEN

# Проверить статус контейнеров
docker compose ps
```

### Ошибка подключения к БД

```bash
# Проверить статус PostgreSQL
docker compose ps postgres

# Проверить логи PostgreSQL
docker compose logs postgres

# Перезапустить PostgreSQL
docker compose restart postgres
```

### Очистка и переустановка

```bash
# Остановить и удалить все
docker compose down -v

# Удалить образы
docker image prune -a

# Пересобрать с нуля
docker compose build --no-cache
docker compose up -d
```

---

## 🔐 Безопасность

### Защита .env файла

```bash
# Установите правильные права
chmod 600 .env

# Убедитесь, что .env в .gitignore (если используете Git)
```

### Firewall (опционально)

```bash
# Установка UFW
sudo apt install -y ufw

# Разрешить SSH
sudo ufw allow 22/tcp

# Разрешить HTTP/HTTPS (если нужно)
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## 📊 Мониторинг

### Использование ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Информация о контейнере
docker inspect zodiac_bot
```

### Резервное копирование

```bash
# Бэкап базы данных
docker compose exec postgres pg_dump -U bot botdb > backup_$(date +%Y%m%d).sql

# Бэкап файлов
tar -czf backup_data_$(date +%Y%m%d).tar.gz data/
```

---

## 📞 Быстрая справка

**Все команды в одном месте:**

```bash
# Перезапуск после изменений
docker compose up -d --build --force-recreate bot && docker compose logs -f bot

# Просмотр последних ошибок
docker compose logs --tail=100 bot | grep -i error

# Полная перезагрузка
docker compose down && docker compose up -d --build && docker compose logs -f bot

# Статус и ресурсы
docker compose ps && docker stats --no-stream
```

---

## ✅ Чеклист развертывания

- [ ] Подключение к VPS через Termius установлено
- [ ] Docker и Docker Compose установлены
- [ ] Проект загружен на сервер
- [ ] Файл `.env` создан и настроен
- [ ] Файл `data/predictions.json` на месте
- [ ] Контейнеры успешно запущены (`docker compose ps`)
- [ ] Логи не показывают ошибок
- [ ] Бот отвечает на команду `/start` в Telegram

---

## 🎯 Готово!

Теперь ваш бот работает на VPS. Он будет автоматически запускаться при перезагрузке сервера (благодаря `restart: unless-stopped`).

Для обновления кода просто выполняйте:
```bash
cd ~/projects/tg-zodiac
git pull  # или загрузите новые файлы
docker compose up -d --build --force-recreate bot
```

**Удачи! 🚀**


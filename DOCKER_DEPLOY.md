# 🐳 Развертывание в Docker

## 📋 Требования

- Docker (версия 20.10+)
- Docker Compose (версия 1.29+)

## 🚀 Первоначальное развертывание

### 1. Подготовка окружения

1. Создайте файл `.env` в корне проекта:
```bash
TG_TOKEN=your_telegram_bot_token
DATABASE_URL=postgresql+asyncpg://bot:secret@postgres:5432/botdb
ADMIN_ID=375961707,498458650
DAILY_HOUR=9
DAILY_MINUTE=0
```

**Важно:** 
- Замените `your_telegram_bot_token` на реальный токен бота
- Замените `ADMIN_ID` на ваши ID администраторов
- Если используете PostgreSQL, настройте `DATABASE_URL` (в docker-compose уже настроен)

### 2. Подготовка данных

Убедитесь, что файл `data/predictions.json` содержит все 31 день предсказаний для всех 12 знаков зодиака.

### 3. Запуск

```bash
# Сборка и запуск контейнеров
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f bot
```

## 🔄 Управление контейнерами

### Просмотр статуса

```bash
# Статус всех контейнеров
docker-compose ps

# Логи бота
docker-compose logs -f bot

# Логи PostgreSQL
docker-compose logs -f postgres

# Логи всех сервисов
docker-compose logs -f
```

### Остановка

```bash
# Остановка всех контейнеров (graceful shutdown)
docker-compose stop

# Остановка с удалением контейнеров
docker-compose down

# Остановка с удалением контейнеров и volumes (⚠️ Удалит БД!)
docker-compose down -v
```

### Перезапуск

```bash
# Перезапуск бота (PostgreSQL остается запущенным)
docker-compose restart bot

# Перезапуск всех сервисов
docker-compose restart
```

## 🔧 Обновление кода

### Вариант 1: Пересборка образа

```bash
# Остановить контейнер
docker-compose stop bot

# Пересобрать образ с новым кодом
docker-compose build bot

# Запустить заново
docker-compose up -d bot

# Или одной командой:
docker-compose up -d --build bot
```

### Вариант 2: Быстрое обновление (если изменения только в коде)

```bash
# Обновить код и перезапустить
docker-compose up -d --build --force-recreate bot
```

### Вариант 3: Полное обновление

```bash
# Остановить и удалить контейнеры
docker-compose down

# Пересобрать образы
docker-compose build --no-cache

# Запустить заново
docker-compose up -d
```

## 📝 Обновление файлов данных

### Обновление predictions.json

```bash
# 1. Отредактируйте файл data/predictions.json
# 2. Перезапустите бота для применения изменений
docker-compose restart bot
```

Файл `data/predictions.json` монтируется как volume, поэтому изменения применяются сразу после перезапуска.

### Обновление .env файла

```bash
# 1. Отредактируйте .env файл
# 2. Перезапустите контейнеры
docker-compose down
docker-compose up -d
```

## 🔍 Мониторинг и отладка

### Вход в контейнер бота

```bash
# Интерактивная оболочка
docker-compose exec bot bash

# Или напрямую
docker exec -it zodiac_bot bash
```

### Проверка подключения к БД

```bash
# Войти в PostgreSQL
docker-compose exec postgres psql -U bot -d botdb

# Выполнить SQL запросы
docker-compose exec postgres psql -U bot -d botdb -c "SELECT COUNT(*) FROM users;"
```

### Просмотр логов с фильтрацией

```bash
# Логи за последние 100 строк
docker-compose logs --tail=100 bot

# Логи с фильтром по ошибкам
docker-compose logs bot | grep -i error

# Логи за последний час
docker-compose logs --since 1h bot
```

### Проверка использования ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Детальная информация о контейнере
docker inspect zodiac_bot
```

## 🗄️ Резервное копирование

### Бэкап базы данных

```bash
# Создать бэкап
docker-compose exec postgres pg_dump -U bot botdb > backup_$(date +%Y%m%d_%H%M%S).sql

# Или с сжатием
docker-compose exec postgres pg_dump -U bot botdb | gzip > backup_$(date +%Y%m%d_%H%M%S).sql.gz
```

### Восстановление из бэкапа

```bash
# Восстановить из файла
docker-compose exec -T postgres psql -U bot botdb < backup_20241201_120000.sql

# Или из сжатого
gunzip < backup_20241201_120000.sql.gz | docker-compose exec -T postgres psql -U bot botdb
```

### Бэкап файлов данных

```bash
# Скопировать файлы из контейнера
docker cp zodiac_bot:/app/data ./backup_data_$(date +%Y%m%d_%H%M%S)

# Или просто скопировать локальную папку data
cp -r data backup_data_$(date +%Y%m%d_%H%M%S)
```

## 🔐 Безопасность

### Обновление паролей PostgreSQL

1. Отредактируйте `docker-compose.yml` (измените `POSTGRES_PASSWORD`)
2. Обновите `DATABASE_URL` в `.env`
3. Пересоздайте контейнеры:

```bash
docker-compose down -v  # ⚠️ Удалит данные БД!
docker-compose up -d
```

### Защита .env файла

```bash
# Убедитесь, что .env в .gitignore
# Установите правильные права доступа
chmod 600 .env
```

## 🐛 Решение проблем

### Бот не запускается

```bash
# Проверить логи
docker-compose logs bot

# Проверить переменные окружения
docker-compose exec bot env | grep -E "TG_TOKEN|DATABASE_URL|ADMIN_ID"

# Проверить подключение к БД
docker-compose exec bot python -c "from database import engine; print(engine)"
```

### Очистка неиспользуемых ресурсов

```bash
# Удалить остановленные контейнеры
docker-compose rm -f

# Очистить неиспользуемые образы
docker image prune -a

# Полная очистка (⚠️ Осторожно!)
docker system prune -a --volumes
```

### Проблемы с volumes

```bash
# Просмотр volumes
docker volume ls

# Удаление volume с БД (⚠️ Удалит данные!)
docker volume rm tg-zodiac_pgdata
```

## 📊 Полезные команды

### Однострочные команды для быстрого управления

```bash
# Перезапуск с пересборкой
docker-compose up -d --build --force-recreate bot && docker-compose logs -f bot

# Просмотр последних ошибок
docker-compose logs --tail=50 bot | grep -i error

# Проверка статуса
docker-compose ps && echo "---" && docker stats --no-stream

# Полная перезагрузка (stop + rm + build + up)
docker-compose down && docker-compose up -d --build && docker-compose logs -f bot
```

## 🔄 Автоматическое обновление (опционально)

Для автоматического обновления при изменении кода можно использовать watch или inotify:

```bash
# Установить entr (Linux) или используйте watch-tools
# Затем запустить:
while true; do
  find . -name "*.py" -mtime -1 | entr -d docker-compose restart bot
done
```

## 📦 Структура проекта в Docker

```
/app/                    # Рабочая директория в контейнере
├── bot.py              # Основной файл бота
├── scheduler.py        # Планировщик рассылок
├── database.py         # Модели БД
├── config.py           # Конфигурация
├── resilience.py       # Отказоустойчивость
└── data/               # Монтируется из ./data (локально)
    └── predictions.json
```

## ⚙️ Переменные окружения

Все переменные из `.env` файла автоматически загружаются в контейнер.

Основные переменные:
- `TG_TOKEN` - токен Telegram бота (обязательно)
- `DATABASE_URL` - URL базы данных (опционально, есть default)
- `ADMIN_ID` или `ADMIN_IDS` - ID администраторов через запятую
- `DAILY_HOUR` - час рассылки (по умолчанию 9)
- `DAILY_MINUTE` - минута рассылки (по умолчанию 0)

## 🎯 Чеклист перед запуском

- [ ] Создан файл `.env` с правильными значениями
- [ ] Файл `data/predictions.json` содержит данные для 31 дня
- [ ] Docker и Docker Compose установлены
- [ ] Порты 5432 (PostgreSQL) не заняты другими приложениями
- [ ] Права доступа к папке `data/` корректные

## 📞 Поддержка

При возникновении проблем проверьте:
1. Логи: `docker-compose logs -f bot`
2. Статус контейнеров: `docker-compose ps`
3. Переменные окружения: `docker-compose exec bot env`


# 📤 Загрузка проекта на GitHub

## 🚀 Быстрая инструкция

### Шаг 1: Проверьте Git конфигурацию

```bash
git config user.name
git config user.email
```

Если пусто, настройте:
```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш@email.com"
```

### Шаг 2: Добавьте все файлы

```bash
git add .
```

### Шаг 3: Сделайте первый коммит

```bash
git commit -m "Initial commit: Telegram bot for zodiac horoscopes"
```

### Шаг 4: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Введите имя: `tg-zodiac` (или любое другое)
4. **НЕ** включайте README, .gitignore или лицензию (у нас уже есть)
5. Нажмите **"Create repository"**

### Шаг 5: Подключите удаленный репозиторий

```bash
git remote add origin https://github.com/YOUR_USERNAME/tg-zodiac.git
```

**Замените `YOUR_USERNAME` на ваш GitHub username!**

### Шаг 6: Загрузите на GitHub

```bash
git branch -M main
git push -u origin main
```

Если будет запрошен логин/пароль, используйте:
- **Username**: ваш GitHub username
- **Password**: Personal Access Token (не пароль от аккаунта!)

### Как получить Personal Access Token:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (full control)
4. Скопируйте токен (показывается только один раз!)

---

## 🔍 Проверка

После загрузки откройте ваш репозиторий на GitHub и проверьте, что все файлы на месте.

**Важно:** Убедитесь, что файл `.env` **НЕ** загружен в репозиторий!

---

## 📝 Команды для последующих обновлений

```bash
# Добавить изменения
git add .

# Сделать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

---

## ⚠️ Если что-то пошло не так

### Отменить последний коммит (но сохранить изменения)
```bash
git reset --soft HEAD~1
```

### Удалить удаленный репозиторий
```bash
git remote remove origin
```

### Перезаписать историю (осторожно!)
```bash
git push -f origin main
```

---

## ✅ Чеклист перед загрузкой

- [ ] `.env` файл не добавлен в git (проверьте: `git status`)
- [ ] База данных `*.db` не добавлена в git
- [ ] README.md создан
- [ ] Все нужные файлы на месте
- [ ] Git конфигурация настроена

---

**Удачи! 🚀**


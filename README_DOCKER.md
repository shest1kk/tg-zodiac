# 🐳 Быстрый старт с Docker

## 🚀 Быстрое развертывание

### 1. Подготовка

Создайте файл `.env`:
```env
TG_TOKEN=your_bot_token_here
ADMIN_ID=375961707,498458650
DAILY_HOUR=9
DAILY_MINUTE=0
```

### 2. Запуск

```bash
docker-compose up -d --build
```

### 3. Проверка

```bash
# Смотреть логи
docker-compose logs -f bot

# Статус
docker-compose ps
```

## 📝 Основные команды

См. файл `DOCKER_DEPLOY.md` для полной документации.

**Быстрая справка:**
- `docker-compose up -d --build` - запуск
- `docker-compose restart bot` - перезапуск бота
- `docker-compose logs -f bot` - логи в реальном времени
- `docker-compose stop` - остановка
- `docker-compose down` - остановка и удаление контейнеров

## 🔄 Обновление после изменения кода

```bash
docker-compose up -d --build --force-recreate bot
```

## 📂 Структура

```
.
├── docker-compose.yml    # Конфигурация Docker Compose
├── Dockerfile           # Образ контейнера
├── .env                # Переменные окружения (создать)
├── data/               # Данные (монтируются в контейнер)
│   └── predictions.json
└── ...                 # Остальные файлы проекта
```

